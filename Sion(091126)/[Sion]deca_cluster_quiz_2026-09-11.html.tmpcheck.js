var TITLE = "[Sion] Deca Cluster Quiz";var QUIZ_KEY = "quiz_siondecacluster_20_2026-09-11_v1";var ATT_KEY = "quiz_siondecacluster_20_att_2026-09-11_v1";var REPORT_ENDPOINT = 'https://script.google.com/macros/s/AKfycbzRYITi7Q4ttOMcqHPBQ_mfc1m-Oyk_ne-7OKeRGbR_WFEUAe9oCKiC9E7f3ymZWR6i/exec';var RAW_QUESTIONS = [{"stem": "During a staff meeting, an employee actively listens, takes notes on action items, and asks clarifying questions before the meeting ends. What does this behavior best demonstrate?", "opts": ["A violation of standard meeting etiquette", "An attempt to dominate the discussion unfairly", "Effective participation in a staff meeting", "Poor preparation for the meeting's agenda"], "ci": 2, "exp": "Active listening, note-taking, and asking clarifying questions are all hallmarks of effective, constructive participation in a staff meeting."}, {"stem": "A manager first identifies that customer complaints have increased, then moves to the next step of the problem-solving process before brainstorming solutions. Which step logically comes immediately after identifying the problem?", "opts": ["Immediately implementing a random solution", "Reassigning blame to the employees involved", "Ignoring the issue until it resolves itself", "Gathering relevant information and data about the problem"], "ci": 3, "exp": "The standard problem-solving process moves from identifying the problem to gathering relevant information before brainstorming and evaluating possible solutions."}, {"stem": "A manufacturing company assigns its overhead costs to specific product lines based on the actual activities that drive those costs, rather than spreading overhead evenly. What is this an example of?", "opts": ["Simply recording routine daily cash transactions", "A non-traditional use of financial information, such as activity-based costing, to improve cost accuracy", "A method for calculating employee payroll taxes", "Standard external financial reporting required for public companies"], "ci": 1, "exp": "Activity-based costing (ABC) is a non-traditional application of financial information that ties overhead costs to the specific activities that actually cause them, giving more accurate product costs than traditional flat allocation."}, {"stem": "A company that extracts raw lumber from forests, a second company that manufactures that lumber into furniture, and a third company that sells the finished furniture to consumers each represent a different business activity. Which three categories do these represent, respectively?", "opts": ["Financing, insuring, and auditing", "Extraction, production/manufacturing, and marketing/distribution", "Advertising, budgeting, and forecasting", "Importing, exporting, and warehousing"], "ci": 1, "exp": "These three businesses represent the classic sequence of business activities: extracting a raw material, manufacturing it into a finished good, and marketing/distributing that good to consumers."}, {"stem": "A competitor spreads false statements claiming a company's product is unsafe, causing the company to lose customers and revenue even though the claims are untrue. Which type of tort does this best describe?", "opts": ["Breach of contract &mdash; failing to fulfill agreed-upon contract terms", "Defamation &mdash; making false statements that damage another party's reputation or business", "Trespass &mdash; unlawfully entering or interfering with another's property", "Negligence &mdash; failing to exercise reasonable care, causing accidental harm"], "ci": 1, "exp": "Knowingly spreading false statements that damage a business's reputation and cause financial harm is the tort of defamation, distinct from contract, negligence, or property-related torts."}, {"stem": "Before a job interview at a financial firm, a candidate makes sure their clothing is clean, professional, and appropriate for the company's industry standards. Why does this matter in a professional context?", "opts": ["Appearance is only relevant for employees in public-facing sales roles", "Companies are legally required to provide identical clothing to every employee", "Personal appearance has no effect on how coworkers or clients perceive an employee", "Appropriate personal appearance helps create a positive professional impression and reflects workplace standards"], "ci": 3, "exp": "Maintaining appropriate personal appearance helps convey professionalism and creates a positive first impression, which matters in nearly every workplace role, not just customer-facing ones."}, {"stem": "A collection of employees works together with a shared goal, clearly defined roles, mutual accountability, and complementary skills to complete a project. How does this best differ from a simple group of coworkers?", "opts": ["Teams and groups are functionally identical in every respect", "Groups will always outperform teams on collaborative projects", "A group always has a designated leader while a team never does", "A team has interdependent members who share accountability for a common goal, unlike a loosely connected group"], "ci": 3, "exp": "The key distinction between a team and a mere group is interdependence and shared accountability toward a common goal, which a loosely connected group of coworkers typically lacks."}, {"stem": "An employee consistently follows internal reporting procedures, discloses potential conflicts of interest, and adheres to company compliance policies. What is this employee doing?", "opts": ["Violating the company's code of conduct", "Undermining the company's normal chain of command", "Fulfilling their role in supporting the company's corporate governance objectives", "Ignoring the company's risk management policies"], "ci": 2, "exp": "Following reporting procedures, disclosing conflicts of interest, and complying with policy are exactly how individual employees support and help achieve corporate governance objectives."}, {"stem": "A new department manager focuses on inspiring her team with a shared long-term vision, coaches individuals based on their specific needs, and adapts her approach to each employee. Which best describes this manager's approach?", "opts": ["Laissez-faire leadership that avoids providing any direction at all", "Micromanagement that closely controls every individual task", "Autocratic leadership that makes all decisions without employee input", "Transformational leadership that motivates and develops employees toward a shared goal"], "ci": 3, "exp": "Inspiring a shared vision and individually developing team members are hallmarks of transformational leadership, which differs from micromanagement, pure autocratic control, or a hands-off laissez-faire style."}, {"stem": "A person is offered $1,000 today or $1,000 five years from now, and a positive interest rate is available. Why is receiving the money today more valuable?", "opts": ["The $1,000 will be worth exactly the same amount either way", "Currency never loses or gains value over time regardless of interest rates", "Money received today can be invested and grow in value before the five years pass", "Future payments are always guaranteed to be paid in full"], "ci": 2, "exp": "This reflects the time value of money: a dollar today can be invested to earn a return, making it worth more than the same dollar received years from now."}, {"stem": "An analyst notices that a company's debt-to-equity ratio has been steadily rising for three years while its interest coverage ratio has been falling. What financial risk does this trend most likely reveal?", "opts": ["Reduced exposure to foreign currency fluctuations", "Increasing risk that the company may struggle to meet its debt obligations", "A decrease in the company's total reported assets", "Improving profitability driven by higher sales volume"], "ci": 1, "exp": "A rising debt-to-equity ratio combined with a falling interest coverage ratio signals growing reliance on debt and a shrinking ability to cover interest payments &mdash; a warning sign of financial risk."}, {"stem": "An accountant is asked by a manager to slightly overstate quarterly revenue so the company appears more profitable to investors. What ethical principle would be violated if the accountant complies?", "opts": ["The principle of honest and accurate financial reporting", "The principle of minimizing accounting paperwork", "The principle of satisfying every manager's request", "The principle of maximizing short-term stock price at any cost"], "ci": 0, "exp": "Accurately and honestly reporting financial results is a foundational ethical principle in accounting; deliberately overstating revenue directly violates it."}, {"stem": "A US-based company evaluates foreign exchange rates, international trade agreements, and cultural differences before opening a new plant overseas. What is the company primarily analyzing?", "opts": ["The company's internal organizational chart", "Its employee vacation and leave policies", "The global business environment factors affecting its international operations", "Only its domestic tax obligations"], "ci": 2, "exp": "Exchange rates, trade agreements, and cultural differences are all part of the global business environment that companies must analyze before expanding internationally."}, {"stem": "A group of factory workers forms a union and negotiates as a group with management over wages and working conditions. What is this negotiation process called?", "opts": ["Outsourcing &mdash; hiring an external company to perform certain business functions", "Collective bargaining &mdash; negotiation between organized workers and employers over employment terms", "Franchising &mdash; licensing a business model to independent operators", "Arbitrage &mdash; profiting from price differences of the same asset in different markets"], "ci": 1, "exp": "When organized labor negotiates as a unified group with an employer over wages and conditions, that process is called collective bargaining."}, {"stem": "A bank employee has access to a customer's confidential financial records and considers sharing some details with a family member out of curiosity. What ethical principle in customer relationship management is at stake?", "opts": ["Maintaining client confidentiality and trust", "Maximizing the bank's quarterly sales targets", "Following the bank's marketing content calendar", "Reducing employee turnover rates at the branch"], "ci": 0, "exp": "Protecting a client's confidential information from unauthorized disclosure is a core ethical responsibility in customer relationship management."}, {"stem": "A financial analyst discovers that reporting a client's investment losses accurately would likely cause the client to switch to a competing firm, but presenting the numbers favorably would violate professional standards. What primarily makes this situation an ethical dilemma?", "opts": ["The client has broken the law and must be reported to the authorities", "The situation is simple because only one option has any real consequence", "The analyst must choose between two conflicting duties &mdash; honesty and business self-interest &mdash; with no clearly satisfying option", "There is a single, mathematically obvious correct number to report"], "ci": 2, "exp": "An ethical dilemma arises specifically when two legitimate obligations (professional honesty and the desire to retain the client's business) conflict, and no choice fully satisfies both."}, {"stem": "When explaining a new financial policy, an employee uses technical jargon and detailed data tables for the finance department, but simple language and visuals for a group of new hires unfamiliar with finance terms. What communication principle is being demonstrated?", "opts": ["Adapting communication style to fit the needs and knowledge level of the target audience", "Using highly technical language for every audience to appear more credible", "Avoiding written communication in favor of verbal-only methods", "Always using identical wording and format regardless of the audience"], "ci": 0, "exp": "Tailoring vocabulary and format to match an audience's background and needs is the principle of employing communication styles appropriate to the target audience."}, {"stem": "Which of the following events would appear on a company's statement of changes in equity?", "opts": ["Recording a customer's unpaid invoice", "Issuing new shares of stock to investors", "Recording depreciation on factory machinery", "Purchasing raw materials for production"], "ci": 1, "exp": "The statement of changes in equity tracks movements in owners' equity, such as new stock issuances, dividends, and net income, not routine operating transactions like invoices, purchases, or depreciation entries."}, {"stem": "A company trains employees at every level to continuously look for ways to improve processes and reduce defects, and rewards staff for identifying quality improvements. What is this company building?", "opts": ["A hierarchy where only managers are responsible for quality", "A culture focused solely on cutting costs regardless of quality", "A quality culture that embeds continuous improvement into everyday operations", "A system that discourages employees from giving feedback"], "ci": 2, "exp": "Training and rewarding employees at all levels to pursue continuous improvement is exactly how a business builds an organization-wide quality culture."}, {"stem": "A customer decides to purchase a particular car brand largely because her close friends and family recommend it. Which factor is most directly influencing her buying behavior?", "opts": ["Social or reference group influence", "Government regulation of the automotive industry", "The company's corporate accounting practices", "The manufacturer's internal production costs"], "ci": 0, "exp": "Recommendations from friends and family are a form of social/reference group influence, a well-known factor that shapes consumer buying behavior."}];var N = RAW_QUESTIONS.length;var ORDER_KEY = QUIZ_KEY + '_order_v1';
function shuffleArr(arr){
  var a = arr.slice();
  for(var i=a.length-1;i>0;i--){
    var j = Math.floor(Math.random()*(i+1));
    var t=a[i]; a[i]=a[j]; a[j]=t;
  }
  return a;
}
function identityOrder(){var o=[];for(var i=0;i<N;i++)o.push(i);return o;}
function isValidOrder(arr){
  if(!Array.isArray(arr) || arr.length===0) return false;
  for(var i=0;i<arr.length;i++){ if(typeof arr[i] !== 'number' || arr[i]<0 || arr[i]>=N) return false; }
  return true;
}
function loadOrder(){
  try{
    var raw = localStorage.getItem(ORDER_KEY);
    if(!raw) return null;
    var arr = JSON.parse(raw);
    if(!isValidOrder(arr)) throw new Error('bad');
    return arr;
  }catch(e){ try{localStorage.removeItem(ORDER_KEY);}catch(e2){} return null; }
}
function saveOrder(o){ try{ localStorage.setItem(ORDER_KEY, JSON.stringify(o)); }catch(e){} }

var order = loadOrder() || identityOrder();
if(!loadOrder()) saveOrder(order);
var pendingFreshMode = 'original';

function defaultState(){
  return { cur:0, answers:{}, showResults:false, timerTotalSec:null, timerStartedAt:null };
}
function loadState(){
  try{
    var raw = localStorage.getItem(QUIZ_KEY);
    if(!raw) return defaultState();
    var s = JSON.parse(raw);
    if(typeof s.cur !== 'number' || s.cur<0 || s.cur>=order.length) s.cur = 0;
    if(typeof s.answers !== 'object' || s.answers===null) s.answers = {};
    if(typeof s.showResults !== 'boolean') s.showResults = false;
    if(typeof s.timerTotalSec === 'undefined') s.timerTotalSec = null;
    if(typeof s.timerStartedAt === 'undefined') s.timerStartedAt = null;
    return s;
  }catch(e){ try{localStorage.removeItem(QUIZ_KEY);}catch(e2){} return defaultState(); }
}
function saveState(){ try{ localStorage.setItem(QUIZ_KEY, JSON.stringify(state)); }catch(e){} }

var state = loadState();
var timerInterval = null;

function getAttempts(){ try{ return parseInt(localStorage.getItem(ATT_KEY)||'0',10); }catch(e){ return 0; } }
function incAttempts(){ try{ localStorage.setItem(ATT_KEY, String(getAttempts()+1)); }catch(e){} }
function updateAttDisplay(){
  var el = document.getElementById('att-display');
  if(el) el.textContent = 'Total Attempts: ' + getAttempts();
}

function formatTime(sec){
  sec = Math.max(0, Math.round(sec));
  var m = Math.floor(sec/60), s = sec%60;
  return String(m).padStart(2,'0')+':'+String(s).padStart(2,'0');
}
function renderTimer(){
  var setupEl = document.getElementById('timerSetup');
  var runningEl = document.getElementById('timerRunning');
  var dispEl = document.getElementById('timerDisplay');
  if(!setupEl||!runningEl||!dispEl) return;
  if(!state.timerStartedAt){ setupEl.style.display='flex'; runningEl.style.display='none'; return; }
  setupEl.style.display='none'; runningEl.style.display='flex';
  var elapsed = (Date.now()-state.timerStartedAt)/1000;
  var remaining = state.timerTotalSec - elapsed;
  dispEl.classList.remove('warn','danger');
  if(remaining<=0){ dispEl.textContent="Time's Up"; dispEl.classList.add('danger'); stopTimerInterval(); return; }
  dispEl.textContent = formatTime(remaining);
  if(remaining <= state.timerTotalSec*0.1) dispEl.classList.add('danger');
  else if(remaining <= state.timerTotalSec*0.25) dispEl.classList.add('warn');
}
function startTimer(){
  var input = document.getElementById('timerMinutesInput');
  var minutes = parseFloat(input.value);
  if(!minutes || minutes<=0) minutes=20;
  state.timerTotalSec = Math.round(minutes*60);
  state.timerStartedAt = Date.now();
  saveState(); renderTimer(); startTimerInterval();
}
function startTimerInterval(){ stopTimerInterval(); timerInterval=setInterval(renderTimer,1000); }
function stopTimerInterval(){ if(timerInterval){clearInterval(timerInterval); timerInterval=null;} }
function resetTimer(){
  state.timerTotalSec = null; state.timerStartedAt = null;
  stopTimerInterval(); saveState(); renderTimer();
}

function render(){
  updateAttDisplay();
  var qa = document.getElementById('quiz-area');
  var rs = document.getElementById('results-screen');
  if(state.showResults){
    qa.style.display='none';
    rs.classList.add('show');
    renderResults();
    return;
  }
  qa.style.display='block';
  rs.classList.remove('show');

  var qi = order[state.cur];
  var q = RAW_QUESTIONS[qi];
  document.getElementById('q-num').textContent = 'Q' + (state.cur+1) + ' / ' + order.length;

  var badge = document.getElementById('tag-badge');
  if(q.tag){
    badge.style.display='inline-block';
    badge.textContent = q.tag;
    badge.style.background = q.tagcolor || '#185FA5';
  } else { badge.style.display='none'; }

  var pbox = document.getElementById('passage-box');
  if(q.passage){ pbox.style.display='block'; pbox.innerHTML = q.passage; } else { pbox.style.display='none'; }
  var cbox = document.getElementById('context-box');
  if(q.context){ cbox.style.display='block'; cbox.innerHTML = q.context; } else { cbox.style.display='none'; }

  document.getElementById('q-text').innerHTML = q.stem;

  var answered = state.answers[qi];
  var optDiv = document.getElementById('options');
  optDiv.innerHTML='';
  q.opts.forEach(function(optText, idx){
    var btn = document.createElement('button');
    btn.className='opt-btn';
    btn.innerHTML = optText;
    btn.disabled = answered !== undefined;
    if(answered !== undefined){
      if(idx === q.ci) btn.classList.add(answered===idx ? 'correct' : 'revealed');
      else if(answered === idx) btn.classList.add('wrong');
    }
    btn.onclick = function(){ chooseAnswer(idx); };
    optDiv.appendChild(btn);
  });

  var expDiv = document.getElementById('exp-box');
  if(answered !== undefined && answered !== q.ci){
    expDiv.style.display='block';
    expDiv.innerHTML = q.exp;
  } else {
    expDiv.style.display='none';
  }

  var nextBtn = document.getElementById('next-btn');
  nextBtn.textContent = (state.cur >= order.length-1) ? 'Show Results →' : 'Next →';

  updateNav();
  if(window.postRenderHook) window.postRenderHook();

}

function updateNav(){
  var answered=0;
  for(var k=0;k<order.length;k++){ if(state.answers[order[k]]!==undefined) answered++; }
  document.getElementById('nav-count').textContent = answered + ' / ' + order.length;
  document.getElementById('prog-fill').style.width = (answered/order.length*100).toFixed(1)+'%';
  renderDots();
}
function renderDots(){
  var panel = document.getElementById('dot-panel');
  if(panel.style.display==='none') return;
  var html='';
  for(var pos=0; pos<order.length; pos++){
    var qi = order[pos];
    var bg = state.answers[qi]===undefined ? '#d7dde6' : (state.answers[qi]===RAW_QUESTIONS[qi].ci ? '#185FA5' : '#E24B4A');
    html += '<button class="dot" style="background:'+bg+';" onclick="goToQ('+pos+')">'+(pos+1)+'</button>';
  }
  var answered=0;
  for(var k=0;k<order.length;k++){ if(state.answers[order[k]]!==undefined) answered++; }
  if(answered===order.length){
    html += '<br><button class="results-pill" onclick="showResultsNow()">Results</button>';
  }
  panel.innerHTML = html;
}
function toggleJump(){
  var panel = document.getElementById('dot-panel');
  var btn = document.getElementById('jump-btn');
  if(panel.style.display==='none' || panel.style.display===''){
    panel.style.display='block'; btn.innerHTML='Hide &#9652;'; renderDots();
  } else {
    panel.style.display='none'; btn.innerHTML='Jump &#9662;';
  }
}
function goToQ(pos){
  state.cur = pos; state.showResults=false; saveState(); render();
}
function showResultsNow(){
  state.showResults = true; incAttempts(); saveState(); render(); reportResult();
}
function chooseAnswer(idx){
  var qi = order[state.cur];
  if(state.answers[qi] !== undefined) return;
  state.answers[qi] = idx;
  saveState(); render();
}
function goPrev(){
  if(state.cur>0){ state.cur--; saveState(); render(); }
}
function goNext(){
  if(state.cur < order.length-1){ state.cur++; saveState(); render(); }
  else { state.showResults=true; incAttempts(); saveState(); render(); reportResult(); }
}
function reportResult(){
  try{
    var correct=0, wrong=0, skipped=0;
    for(var pos=0; pos<order.length; pos++){
      var qi = order[pos];
      if(state.answers[qi]===undefined){ skipped++; continue; }
      if(state.answers[qi]===RAW_QUESTIONS[qi].ci) correct++; else wrong++;
    }
    var total = order.length;
    var childName = 'Unknown';
    if(TITLE.indexOf('[Sion]')===0) childName='Sion';
    else if(TITLE.indexOf('[Grant]')===0) childName='Grant';
    else if(TITLE.indexOf('[Both]')===0) childName='Both';
    else if(TITLE.indexOf('[Review]')===0) childName='Review';
    var payload = {
      child: childName,
      quizTitle: TITLE,
      quizKey: QUIZ_KEY,
      attemptNumber: getAttempts(),
      correct: correct, wrong: wrong, skipped: skipped, total: total,
      scorePercent: total>0 ? Math.round((correct/total)*1000)/10 : 0
    };
    fetch(REPORT_ENDPOINT, {
      method: 'POST',
      mode: 'no-cors',
      headers: {'Content-Type': 'text/plain;charset=utf-8'},
      body: JSON.stringify(payload)
    }).catch(function(){});
  }catch(e){}
}
function renderResults(){
  var correct=0, wrong=0, skipped=0;
  for(var pos=0; pos<order.length; pos++){
    var qi = order[pos];
    if(state.answers[qi]===undefined){ skipped++; continue; }
    if(state.answers[qi]===RAW_QUESTIONS[qi].ci) correct++; else wrong++;
  }
  document.getElementById('results-score').textContent = correct+' correct, '+wrong+' wrong, '+skipped+' skipped - out of '+order.length;
  var grid = document.getElementById('results-grid');
  grid.innerHTML='';
  for(var pos=0; pos<order.length; pos++){
    var qi = order[pos];
    var card = document.createElement('div');
    if(state.answers[qi]===undefined){
      card.className='r-card skipped';
      card.textContent='Q'+(pos+1)+' Skip';
    } else {
      var ok = state.answers[qi]===RAW_QUESTIONS[qi].ci;
      card.className='r-card '+(ok?'correct':'wrong');
      card.textContent='Q'+(pos+1)+' '+(ok?'OK':'X');
    }
    (function(p){ card.onclick=function(){ state.showResults=false; state.cur=p; saveState(); render(); }; })(pos);
    grid.appendChild(card);
  }
}
function retryWrong(){
  var wrong=[];
  for(var pos=0; pos<order.length; pos++){
    var qi = order[pos];
    if(state.answers[qi]!==undefined && state.answers[qi]!==RAW_QUESTIONS[qi].ci){
      wrong.push(qi);
    }
  }
  if(wrong.length===0) return;
  wrong.forEach(function(qi){ delete state.answers[qi]; });
  order = wrong;
  saveOrder(order);
  state.cur=0; state.showResults=false;
  saveState(); render(); renderTimer();
}
function copyRes(){
  var lines=[TITLE+' Results'];
  for(var pos=0; pos<order.length; pos++){
    var qi = order[pos];
    var a = state.answers[qi];
    var s='Q'+(pos+1)+': ';
    if(a===undefined) s+='Skipped';
    else s += (a===RAW_QUESTIONS[qi].ci ? 'Correct' : 'Wrong');
    lines.push(s);
  }
  var text = lines.join('\n');
  try{
    if(navigator.clipboard){ navigator.clipboard.writeText(text).catch(function(){}); }
    else { var ta=document.createElement('textarea'); ta.value=text; document.body.appendChild(ta); ta.select(); document.execCommand('copy'); document.body.removeChild(ta); }
  }catch(e){}
}
function saveWrongAnswers(){
  var wrongItems=[];
  for(var pos=0; pos<order.length; pos++){
    var qi = order[pos];
    var a = state.answers[qi];
    if(a!==undefined && a!==RAW_QUESTIONS[qi].ci){
      var q = RAW_QUESTIONS[qi];
      wrongItems.push({ question: q.stem, yourAnswer: q.opts[a], correctAnswer: q.opts[q.ci], explanation: q.exp });
    }
  }
  var blob = new Blob([JSON.stringify({title:TITLE, wrongAnswers:wrongItems}, null, 2)], {type:'application/json'});
  var url = URL.createObjectURL(blob);
  var a2 = document.createElement('a');
  a2.href = url; a2.download = QUIZ_KEY + '_wrong.json';
  document.body.appendChild(a2); a2.click(); document.body.removeChild(a2);
  URL.revokeObjectURL(url);
}
function openFreshModal(mode){
  pendingFreshMode = mode;
  var t = document.getElementById('freshModalTitle');
  var m = document.getElementById('freshModalText');
  if(mode==='random'){
    t.textContent='Start Fresh (Random Order)?';
    m.textContent='Questions will be reshuffled into a new random order, and all progress will be cleared. This cannot be undone.';
  } else {
    t.textContent='Start Fresh (Original Order)?';
    m.textContent='Questions will return to their original order, and all progress will be cleared. This cannot be undone.';
  }
  document.getElementById('freshModal').classList.add('show');
}
function openFreshModalOriginal(){ openFreshModal('original'); }
function openFreshModalRandom(){ openFreshModal('random'); }
function closeFreshModal(){ document.getElementById('freshModal').classList.remove('show'); }
function confirmFresh(){
  order = (pendingFreshMode==='random') ? shuffleArr(identityOrder()) : identityOrder();
  saveOrder(order);
  state = defaultState();
  saveState();
  resetTimer();
  closeFreshModal();
  render();
  renderTimer();
}

window.startTimer=startTimer;
window.toggleJump=toggleJump;
window.goToQ=goToQ;
window.showResultsNow=showResultsNow;
window.chooseAnswer=chooseAnswer;
window.goPrev=goPrev;
window.goNext=goNext;
window.retryWrong=retryWrong;
window.copyRes=copyRes;
window.saveWrongAnswers=saveWrongAnswers;
window.openFreshModalOriginal=openFreshModalOriginal;
window.openFreshModalRandom=openFreshModalRandom;
window.closeFreshModal=closeFreshModal;
window.confirmFresh=confirmFresh;

render();
renderTimer();
if(state.timerStartedAt) startTimerInterval();

